/** Cart / checkout: free shipping when subtotal >= this (INR). */
export const FREE_SHIPPING_MIN_INR = 5999;

/** Flat shipping when below free-shipping threshold (INR). */
export const SHIPPING_FLAT_INR = 99;
