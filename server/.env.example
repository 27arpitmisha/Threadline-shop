PORT=5001
MONGODB_URI=mongodb://127.0.0.1:27017/threadline
JWT_SECRET=change-this-to-a-long-random-string-in-production
# Optional: this email gets role "admin" on register/login (product uploads at /admin)
ADMIN_EMAIL=akashrajawat1111@gmail.com
# CORS: dev default is localhost:5173. For split hosting use your frontend URL(s), comma-separated.
# CLIENT_ORIGIN=http://localhost:5173
# Optional: absolute path to Vite dist if not ../client/dist from server cwd
# CLIENT_DIST_PATH=
